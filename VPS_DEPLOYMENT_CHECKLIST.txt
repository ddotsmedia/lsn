╔════════════════════════════════════════════════════════════════════════════╗
║                    ADMIN PANEL LOGIN - VPS DEPLOYMENT                      ║
║                          Action Checklist                                   ║
╚════════════════════════════════════════════════════════════════════════════╝

STATUS: Ready to deploy on VPS ✅

WHAT'S FIXED:
✅ Backend login endpoint (500 error issue)
✅ Docker config for auto-migrations
✅ Database initialization setup

WHAT YOU NEED TO DO (Run these on VPS):

┌─ STEP 1: Update Code ─────────────────────────────────────────────────────┐
│ cd /opt/lsn                                                                │
│ git pull origin main                                                       │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ STEP 2: Reset Database ──────────────────────────────────────────────────┐
│ docker compose -f docker-compose.prod.yml down                             │
│ docker volume rm lsn_postgres_prod                                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ STEP 3: Rebuild & Start ─────────────────────────────────────────────────┐
│ docker compose -f docker-compose.prod.yml build --no-cache backend         │
│ docker compose -f docker-compose.prod.yml up -d                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ STEP 4: Wait for Postgres ───────────────────────────────────────────────┐
│ # Watch initialization (should show migration messages)                    │
│ docker compose -f docker-compose.prod.yml logs postgres                    │
│                                                                             │
│ # Confirm ready (should say "accepting connections")                       │
│ docker compose -f docker-compose.prod.yml exec postgres pg_isready \       │
│   -U lsn -d littlesmarties                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ STEP 5: Create Admin User ───────────────────────────────────────────────┐
│ docker compose -f docker-compose.prod.yml exec postgres psql \             │
│   -U lsn -d littlesmarties << 'EOSQL'                                      │
│ INSERT INTO users (id, email, name, password_hash, phone)                  │
│ VALUES (                                                                   │
│   gen_random_uuid(),                                                       │
│   'admin@test.local',                                                      │
│   'Test Admin',                                                            │
│   '$2b$10$YJE8Nf1L8Z0M0Q5K1V7X9.5Z0A8B1C2D3E4F5G6H7I8J9K0L1M2N3O4P5Q6', │
│   '+1234567890'                                                            │
│ )                                                                          │
│ ON CONFLICT (email) DO NOTHING;                                            │
│ EOSQL                                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ STEP 6: Test It Works ───────────────────────────────────────────────────┐
│ # Test API (should return 200 with tokens, NOT 500)                        │
│ curl -X POST http://localhost:3001/api/v1/auth/login \                    │
│   -H "Content-Type: application/json" \                                    │
│   -d '{"email":"admin@test.local","password":"test123"}'                   │
│                                                                             │
│ # Test browser                                                             │
│ Visit: https://bayrotna.ae/admin                                          │
│ Login: admin@test.local / test123                                          │
│                                                                             │
│ ✅ Should work without 500 error!                                          │
└─────────────────────────────────────────────────────────────────────────────┘

HELP:
- Full guide: DEPLOYMENT_MIGRATION_FIX.md
- Summary: DEPLOYMENT_SUMMARY.md
- Questions: Check "Troubleshooting" section in deployment guide

KEY CHANGES:
- docker-compose.prod.yml: Added migration volume mounts
- scripts/postgres-init.sh: New init script
- authController.ts: Fixed refresh token duplicate key error
